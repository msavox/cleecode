% Caso 2 — file Octave: il pulsante mostra "octave", e il menu apre il
% comando configurato per i .m (octave --persist {file}).
x = linspace(0, 2*pi, 200);
printf("media di sin(x): %f\n", mean(sin(x)));
figure(1)
plot(x, x));
